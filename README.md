# Create a FKing Migration

in package manager console:

set default project to 'src\infrastructure\Persistence'

then run this command:

```cmd
Add-Migration AddTimelineTable -Context ApplicationDbContext
```

then the Fking Migration is creates right away!
